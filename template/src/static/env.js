/**
 * 总环境变量，通常配这一个地方即可，后面那些变量只有在不采用默认值需要另外定义的情况下才需要配置
 * 如：1生产环境 2预发布环境 3测试环境 4开发环境 5数据mock环境
 */
window._ENV = '1'

/**
 * 服务A接口前缀
 */
window.apiPrefixForServerA = ''

/**
 * 服务B接口前缀
 */
window.apiPrefixForServerB = ''
